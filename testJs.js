window.app = () => {
    const react = require('react');
    console.log('Got this awesome javascript!!');
    return () => {
        return (
            <div>
                WOW
            </div>
        );
    };
};
